# Download Sample Set

- Small HTTP file
- Large resumable HTTP file
- HTTP file requiring `referer`
- Magnet metadata-only sample
- Torrent with multi-file selection
- HLS manifest sample
- Failure sample returning `403`
- Failure sample with expired signed URL

For each release, record:

- Add path used: direct add / browser bridge / local API / CLI
- Result: completed / failed / retried
- Final health reason
- Manual notes
