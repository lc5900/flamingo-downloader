# Backlog

## Media UX Follow-ups

- Media task independent type model (`direct file`, `HLS`, `DASH`, `audio-only`, `unknown stream`)
- Pre-create manifest inspection for bitrate, audio track, subtitle, and encryption hints
- Rich media chooser for stream variants and output container
- More explicit expired-candidate refresh loop
- Stronger provenance view for source page, manifest, headers summary, and ffmpeg arguments

## Advanced Information Architecture

- Global command palette
- Rich query grammar such as `status:error domain:example.com type:video size:>1GB`
- History analytics dashboard
- Archive view for completed tasks
- Multi-thousand-task performance profiling beyond the current table layout and filters
